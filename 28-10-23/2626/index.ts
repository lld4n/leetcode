// type Fn = (accum: number, curr: number) => number;

// function reduce(nums: number[], fn: Fn, init: number): number {
//   if (nums.length === 0) {
//     return init;
//   } else {
//     let ini = init;
//     for (let el of nums) {
//       ini = fn(ini, el);
//     }
//     return ini;
//   }
// }
